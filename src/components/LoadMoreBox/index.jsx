import {styled} from "@mui/styles";
import {Box} from "@mui/material";

const LoadMoreBox = styled(Box)({
    color: '#0675da', fontWeight: 'bold', cursor: 'pointer', display: 'flex', alignItems: 'center',
});

export default LoadMoreBox;